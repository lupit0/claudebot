# Airline Price Scraper Dependencies

# Google Flights - lightweight, no browser needed
fast-flights>=1.2.0

# Browser automation — pydoll (primary, CDP-based, native CAPTCHA bypass)
pydoll-python>=2.17.0

# Undetected browser automation (Playwright fork) — fallback if pydoll unavailable
patchright>=1.0.0

# Human-like browser behavior
# humanization-playwright>=0.1.0

# HTTP client with retry support
httpx>=0.27.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Data handling
pydantic>=2.0.0

# Output formatting
tabulate>=0.9.0
rich>=13.0.0

# Proxy support
python-dotenv>=1.0.0

# Async support
asyncio-throttle>=1.0.0
